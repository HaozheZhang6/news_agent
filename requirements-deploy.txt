# Deployment requirements (excludes audio libraries that fail on Render)
# Generated for Render deployment without pyaudio, soundfile, librosa, modelscope, webrtcvad

# Core framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0

# Database & Caching  
supabase>=2.10.0
upstash-redis>=0.15.0

# AI & NLP
zhipuai>=2.0.0
edge-tts>=6.0.0

# ASR (Speech Recognition) - Using SenseVoice (same as src implementation)
funasr>=1.2.7

# Utils
pydantic>=2.4.0
pydantic-settings>=2.0.0
httpx>=0.27.0
requests>=2.31.0

# Async support
asyncio-mqtt>=0.16.0
websockets>=12.0

# Existing agent dependencies (from main requirements.txt)
beautifulsoup4>=4.12.0
feedparser>=6.0.10
yfinance>=0.2.0
python-dateutil>=2.8.0
